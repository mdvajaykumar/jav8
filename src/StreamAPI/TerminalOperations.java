package StreamAPI;

public class TerminalOperations {

//    Terminal Operations: These operations produce a result or a side effect and terminate the stream pipeline.
//    Terminal operations include:
//
//    forEach(Consumer): Performs an action for each element.
//    collect(Collector): Transforms the stream into a collection or another data structure.
//    reduce(BinaryOperator): Combines elements to produce a single result.
//    count(): Returns the number of elements.
//    anyMatch(Predicate), allMatch(Predicate), noneMatch(Predicate): Return a boolean based on the predicate.
//    findFirst(), findAny(): Return an Optional describing an element of the stream.

}
